## emit :函数用于触发组件实例上的事件。它的工作原理是将事件名称转换为事件处理函数的键名，然后从组件实例的 props 中获取对应的事件处理函数，并调用该函数。


## slot :插槽是Vue.js中用于在组件中插入内容的机制。它允许开发者在组件中定义一个占位符，然后在组件的使用者中插入内容。插槽可以用于在组件中插入HTML、文本、其他组件等。

## id 为 root 的元素是通过 patch 函数渲染到 DOM 中的。el 属性是在 patch 函数中设置的，而 patch 函数会调用 mountElement 或 patchElement 函数来设置 el 属性。只有在组件被渲染到页面上时，el 属性才会有值。如果 el 没有值，就无法渲染 DOM 元素，也就无法获取 el 的值。，每点击一次按钮，都会创建一个虚拟节点，虚拟dom树，之后通过比对dom树在哪里做了改变，从而只渲染真实发生变化的dom以减少性能损耗

